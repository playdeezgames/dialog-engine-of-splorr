Friend Module Tags
    Friend ReadOnly TOO_HIGH As String = NameOf(TOO_HIGH)
    Friend ReadOnly TOO_LOW As String = NameOf(TOO_LOW)
    Friend ReadOnly QUITTABLE As String = NameOf(QUITTABLE)
End Module
